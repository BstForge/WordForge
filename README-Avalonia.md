# WordForge Avalonia Desktop

## Build and Run

### Windows
Install the .NET 8 SDK and run `dotnet run --project WordForge.Desktop` from a command prompt in the repository root.

### Linux
Ensure the .NET 8 SDK is installed and execute `dotnet run --project WordForge.Desktop` from the repository root.
