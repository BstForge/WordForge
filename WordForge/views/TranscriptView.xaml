<UserControl x:Class="WordForge.Views.TranscriptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WordForge"
             xmlns:vm="clr-namespace:WordForge.ViewModels">
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Column 0 holds the sidebar and collapse ribbon -->
            <ColumnDefinition x:Name="SidebarColumn" Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar for chapters/scenes -->
        <Border Grid.Column="0" Background="#FF1A1B1E" BorderBrush="#FF3B3F45" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#FF1A1B1E" BorderBrush="#FF3B3F45" BorderThickness="0,0,0,1">
                    <DockPanel LastChildFill="True" x:Name="SidebarHeader">
                        <TextBlock x:Name="ChaptersLabel" Text="CHAPTERS" Foreground="#FFE5E7EB" FontWeight="Bold" Margin="8,0,0,0"/>
                        <!-- Toggle collapse/expand -->
                        <Button x:Name="CollapseButton" Content="&lt;" Width="20" Height="20" Margin="2" HorizontalAlignment="Right" Click="CollapseButton_Click" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45"/>
                    </DockPanel>
                </Border>
                <TreeView Grid.Row="1" x:Name="ChapterTree" ItemsSource="{Binding Chapters}" SelectedItemChanged="ChapterTree_SelectedItemChanged" AllowDrop="True" PreviewMouseMove="ChapterTree_PreviewMouseMove" DragOver="ChapterTree_DragOver" Drop="ChapterTree_Drop" PreviewMouseRightButtonDown="ChapterTree_PreviewMouseRightButtonDown" PreviewMouseLeftButtonDown="ChapterTree_PreviewMouseLeftButtonDown" PreviewMouseLeftButtonUp="ChapterTree_PreviewMouseLeftButtonUp" Background="#FF1A1B1E" Foreground="#FFE5E7EB" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <TreeView.Resources>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="Background" Value="#FF1A1B1E"/>
                        </Style>
                        <ContextMenu x:Key="ChapterContextMenu" x:Shared="False" Background="#FF26282C" BorderBrush="#FF3B3F45" Foreground="#FFE5E7EB">
                            <ContextMenu.Resources>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="#FF26282C"/>
                                    <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                    <Setter Property="BorderBrush" Value="#FF3B3F45"/>
                                    <Setter Property="Padding" Value="6,2"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter Property="Background" Value="#FF2F3237"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ContextMenu.Resources>
                            <MenuItem Header="Add Scene" Tag="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Click="AddScene_Click"/>
                            <MenuItem Header="Rename" Tag="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Click="RenameChapter_Click"/>
                            <MenuItem Header="Delete" Tag="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Click="DeleteChapter_Click"/>
                        </ContextMenu>
                        <ContextMenu x:Key="SceneContextMenu" x:Shared="False" Background="#FF26282C" BorderBrush="#FF3B3F45" Foreground="#FFE5E7EB">
                            <ContextMenu.Resources>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="#FF26282C"/>
                                    <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                    <Setter Property="BorderBrush" Value="#FF3B3F45"/>
                                    <Setter Property="Padding" Value="6,2"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter Property="Background" Value="#FF2F3237"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ContextMenu.Resources>
                            <MenuItem Header="Rename" Tag="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Click="RenameScene_Click"/>
                            <MenuItem Header="Delete" Tag="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Click="DeleteScene_Click"/>
                        </ContextMenu>
                        <Style x:Key="ChapterItemStyle" TargetType="TreeViewItem">
                            <Setter Property="ContextMenu" Value="{StaticResource ChapterContextMenu}"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#FFE5E7EB"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                        <Style x:Key="SceneItemStyle" TargetType="TreeViewItem">
                            <Setter Property="ContextMenu" Value="{StaticResource SceneContextMenu}"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#FFE5E7EB"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                        <!-- Chapter template with overflow button -->
                        <HierarchicalDataTemplate DataType="{x:Type local:Chapter}" ItemsSource="{Binding Scenes}" ItemContainerStyle="{StaticResource SceneItemStyle}">
                            <Border x:Name="ChapterBorder" Padding="6,4" BorderThickness="0,0,0,1" BorderBrush="#FF3B3F45">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="ChapterText" Grid.Column="0" Text="{Binding Title, Mode=OneWay}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                    <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                    <TextBox x:Name="ChapterEditBox" Grid.Column="0" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45" BorderThickness="1" KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus"/>
                                    <Button x:Name="ChapterMenuButton" Grid.Column="1" Content="⋮" Width="20" Height="20" Margin="0" Click="OpenItemMenu" Visibility="Hidden" Focusable="True">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                <Setter Property="Background" Value="#FF2F3237"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                <Setter Property="Background" Value="#FF34363B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </HierarchicalDataTemplate>
                        <!-- Scene template with overflow button -->
                        <DataTemplate DataType="{x:Type local:Scene}">
                            <Border x:Name="SceneBorder" Padding="6,4" BorderThickness="0,0,0,1" BorderBrush="#FF3B3F45">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="SceneText" Grid.Column="0" Text="{Binding Title, Mode=OneWay}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                    <Setter Property="Foreground" Value="#FFE5E7EB"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                    <TextBox x:Name="SceneEditBox" Grid.Column="0" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45" BorderThickness="1" KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus"/>
                                    <Button x:Name="SceneMenuButton" Grid.Column="1" Content="⋮" Width="20" Height="20" Margin="0" Click="OpenItemMenu" Visibility="Hidden" Focusable="True">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Hidden"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                <Setter Property="Background" Value="#FF2F3237"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TreeViewItem}}" Value="True">
                                                <Setter Property="Background" Value="#FF34363B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <StaticResource ResourceKey="ChapterItemStyle"/>
                    </TreeView.ItemContainerStyle>
                </TreeView>
                <Border x:Name="AddChapterBar" Grid.Row="2" Background="#FF1A1B1E" BorderBrush="#FF3B3F45" BorderThickness="0,1,0,0">
                    <Button x:Name="AddChapterButton" Content="Add Chapter" Margin="4" Click="AddChapter_Click" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45"/>
                </Border>
            </Grid>
        </Border>

        <!-- Main editor area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="26"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="TRANSCRIPT" Foreground="#FFE5E7EB" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <Button x:Name="InsertBreakButton" Grid.Column="1" Content="Insert Scene Break" Margin="0,0,8,0" Click="InsertBreak_Click" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45"/>
            </Grid>
            <Border x:Name="ChangeBanner" Grid.Row="1" Background="#FF34363B" BorderBrush="#FF3B3F45" BorderThickness="0,0,0,1" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="ChangeBannerText" VerticalAlignment="Center" Foreground="#FFE5E7EB" Margin="8,0"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,0">
                        <Button x:Name="ApplySceneChangesButton" Content="Apply" Margin="0,0,4,0" Click="ApplySceneChanges_Click" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45"/>
                        <Button Content="Dismiss" Click="DismissSceneChanges_Click" Background="#FF26282C" Foreground="#FFE5E7EB" BorderBrush="#FF3B3F45"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="2" Background="#FF26282C" BorderBrush="#FF3B3F45" BorderThickness="1" CornerRadius="4">
                <TextBox x:Name="Editor" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                         Background="#FF26282C" Foreground="#FFE5E7EB" BorderThickness="0"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                         FontSize="14" Padding="10" TextChanged="Editor_TextChanged" PreviewKeyDown="Editor_PreviewKeyDown"/>
            </Border>
            <DockPanel Grid.Row="3" LastChildFill="True" Background="#FF1A1B1E" Margin="2,4,2,0">
                <ComboBox x:Name="ScopeCombo" DockPanel.Dock="Left" Width="100" Margin="6,0" SelectedValuePath="Tag" SelectedValue="{Binding CountScope, Mode=TwoWay}">
                    <ComboBoxItem x:Name="SceneScopeItem" Content="Scene" Tag="{x:Static vm:CountScope.Scene}"/>
                    <ComboBoxItem Content="Chapter" Tag="{x:Static vm:CountScope.Chapter}"/>
                    <ComboBoxItem Content="Project" Tag="{x:Static vm:CountScope.Project}"/>
                </ComboBox>
                <TextBlock x:Name="WordCountText" DockPanel.Dock="Left" Text="Words: 0" Foreground="#FFC9CDD3" Margin="6,0"/>
                <TextBlock x:Name="CharCountText" DockPanel.Dock="Right" Text="Characters: 0" Foreground="#FFC9CDD3" Margin="6,0"/>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
