<UserControl x:Class="WordForge.Menus.TopMenu.CharacterBibleMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:menus="clr-namespace:WordForge.Menus"
             xmlns:top="clr-namespace:WordForge.Menus.TopMenu">
    <Border Background="#FF2A2D31" BorderBrush="#FF0F1012" BorderThickness="0,0,0,1">
        <Grid Margin="12,12,12,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <!-- Hamburger -->
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="140"/>
                <!-- Title 'Right Pane' -->
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="120"/>
                <!-- Timeline -->
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="120"/>
                <!-- Outline -->
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto" MinWidth="320"/>
                <!-- Bibles group -->
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="Auto" MinWidth="400"/>
                <!-- Export group -->
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <!-- Hamburger -->
            <Button x:Name="HamburgerButton" Grid.Column="0" Background="Transparent" BorderBrush="#FF3B3F45" BorderThickness="1" ToolTip="Menu" Cursor="Hand">
                <Image Source="/resources/images/hamburger.png" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>

            <Popup x:Name="HamburgerPopup" PlacementTarget="{Binding ElementName=HamburgerButton}" StaysOpen="False">
                <menus:FileMenu />
            </Popup>

            <!-- Right Pane label -->
            <Border Grid.Column="2" Padding="12,6" Background="#FF2E3136" BorderBrush="#FF3B3F45" BorderThickness="1" CornerRadius="12" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Text="Right Pane" Foreground="#FFE5E7EB" FontWeight="SemiBold" />
            </Border>

            <!-- Timeline button -->
            <Button x:Name="TimelineButton" Grid.Column="4" Height="52" Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                    Foreground="#FF111111" FontWeight="Bold" Cursor="Hand" Content="Timeline"/>

            <!-- Outline button -->
            <Button x:Name="OutlineButton" Grid.Column="6" Height="52" Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                    Foreground="#FF111111" FontWeight="Bold" Cursor="Hand" Content="Outline"/>

            <!-- Bibles group -->
            <Border Grid.Column="8" Background="#FF232529" BorderBrush="#FF3B3F45" BorderThickness="1" CornerRadius="6" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Bibles" Foreground="#FFE5E7EB" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <UniformGrid Grid.Row="2" Rows="1" Columns="3" HorizontalAlignment="Center">
                        <Button x:Name="LocationButton" Width="90" Height="36" Margin="0,0,8,0"
                                Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                                Foreground="#FF111111" FontWeight="SemiBold" Content="Location"/>
                        <Button x:Name="ItemButton" Width="90" Height="36" Margin="0,0,8,0"
                                Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                                Foreground="#FF111111" FontWeight="SemiBold" Content="Item"/>
                        <Button x:Name="LoreButton" Width="90" Height="36"
                                Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                                Foreground="#FF111111" FontWeight="SemiBold" Content="Lore"/>
                    </UniformGrid>
                </Grid>
            </Border>

            <top:ExportGroup Grid.Column="10" />

            <!-- Customize button -->
            <Button x:Name="CustomizeButton" Grid.Column="12" Height="52" Width="120" Background="#FFE5E7EB" BorderBrush="#FF9CA3AF"
                    Foreground="#FF111111" FontWeight="Bold" Content="Customize" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</UserControl>
