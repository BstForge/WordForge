<UserControl x:Class="WordForge.Panes.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*" />
            <ColumnDefinition Width="60*" />
            <ColumnDefinition Width="25*" />
        </Grid.ColumnDefinitions>

        <!-- Left Pane -->
        <StackPanel Grid.Column="0" Background="#EEE" Margin="10">
            <Button Content="New Project" Command="{Binding NewCommand}" />
            <Button Content="Load Project" Command="{Binding LoadCommand}" />
            <TextBlock Text="Recent Projects:" FontWeight="Bold" Margin="0,10,0,5"/>
            <Border CornerRadius="6" BorderBrush="Gray" BorderThickness="1">
                <ListBox ItemsSource="{Binding RecentProjects}" Height="300" Padding="5"
                         SelectedItem="{Binding SelectedRecent, Mode=TwoWay}"
                         SelectionChanged="RecentProjectSelected"/>
            </Border>
        </StackPanel>

        <!-- Center Pane -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Create New Project" FontSize="20" FontWeight="Bold" Margin="0,0,0,30" TextAlignment="Center" HorizontalAlignment="Center"/>
            <StackPanel Width="300">
                <TextBlock Text="Title (required):" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Title, Mode=TwoWay}" />
                <TextBlock Text="Series (optional):" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Series, Mode=TwoWay}" />
                <TextBlock Text="Author (required):" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Author, Mode=TwoWay}" />
                <Button Content="Create" Width="120" HorizontalAlignment="Center" Command="{Binding CreateCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- Right Pane -->
        <StackPanel Grid.Column="2" Margin="10" Background="#F8F8F8" VerticalAlignment="Top">
            <TextBlock Text="Promoted Content" FontWeight="Bold" FontSize="14" Margin="0,10"/>
            <TextBlock Margin="0,10">• My Books on Amazon</TextBlock>
            <TextBlock Margin="0,10">• Goodreads Profile</TextBlock>
            <TextBlock Margin="0,10">• Author Website</TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
